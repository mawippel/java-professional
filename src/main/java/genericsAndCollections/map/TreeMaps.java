package genericsAndCollections.map;

public class TreeMaps {

	/**
	 * Keeps the keys in a sorted tree structure (seems like {@link TreeSet})
	 * Adding and checking has more cost than HashMaps
	 */
	
}
