package genericsAndCollections.collections.queue;

public class ArrayDeque {

	/**
	 * It's a "pure" double-ended queue;
	 * Best type of queue;
	 * 
	 * Can be used as a Queue or a Stack.
	 * 
	 * Stack -> use push()
	 * Queue -> use offer()
	 * 
	 * pop -> pops out the element from the stack/queue
	 * peek -> gets the elements but not remove it from the stack/queue
	 * peekFirst
	 * peekLast
	 * poll -> gets and remove the head of the queue
	 * pollFirst
	 * pollLast
	 */
	
}
