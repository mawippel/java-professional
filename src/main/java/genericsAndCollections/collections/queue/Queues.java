package genericsAndCollections.collections.queue;

public class Queues {

	/**
	 * Used when elements are added/removed in a specific order;
	 * FIFO is the default algorithm;
	 * It's a single-ended queue, which means, it can only add elements at the back of the queue;
	 */
	
}
