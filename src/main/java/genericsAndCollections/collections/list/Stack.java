package genericsAndCollections.collections.list;

public class Stack {

	/**
	 * Used as a stack;
	 * Used to remove elements at the top of the stack;
	 * IF YOU NEED A STACK, USE {@link ArrayDeque}!!!;
	 */
}
