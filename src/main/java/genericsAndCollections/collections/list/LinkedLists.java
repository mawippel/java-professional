package genericsAndCollections.collections.list;

public class LinkedLists {

	/**
	 * Implements List and Queue;
	 * The main benefit is that it implements the List and Queue interfaces;
	 * Good when using it as a Queue;
	 * It's a double-ended queue, which means that we can add/remove elements at the begging or the end of it;
	 */
	
}
